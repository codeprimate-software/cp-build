# Spring Boot application.properties used to configure the Codeprimate Build Tools application and CLI program.

# Spring Shell configuration
spring.shell.interactive.enabled=true

# Logging configuration
logging.level.root=ERROR
logging.level.org.cp.build.tools=INFO
